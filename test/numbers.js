import { describe, it } from "mocha";
import {assert} from "chai";


describe('calculations', function() {
  it('should return correct number sums', function() {
    assert.equal(3 * 4 , 12);
  });
})